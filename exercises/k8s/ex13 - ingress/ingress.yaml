apiVersion: networking.k8s.io/v1
kind: Ingress
metadata:
  annotations:
    cert-manager.io/issuer: letsencrypt-prod
  name: nginx
  namespace: ex13
spec:
  ingressClassName: nginx
  rules:
  - host: 10.10.11.138.nip.io
    http:
      paths:
      - backend:
          service:
            name: nginx
            port:
              number: 80
        path: /
        pathType: Prefix
  tls:
  - hosts:
    - 10.10.11.138.nip.io
    secretName: nipiocert
